<Window x:Class="Keeper.Views.DbFromTxtLoaderProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DbFromTxtLoaderProgressView" Height="300" Width="300">
    <Grid>
       <Grid.RowDefinitions>
           <RowDefinition Height="*"/>
           <RowDefinition Height="30"/>
       </Grid.RowDefinitions>
        <ListBox Margin="5" ItemsSource="{Binding PreviousOperations, Mode=TwoWay}"/>
        <TextBlock Grid.Row="1" Margin="5" Text="{Binding CurrentOperation, Mode=TwoWay}"/>
    </Grid>
</Window>
