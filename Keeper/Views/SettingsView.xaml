<Window x:Class="Keeper.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:Keeper.ViewModels"
        Title="SettingsView" Height="300" Width="700">
    <Grid>
        <ListView ItemsSource="{Binding Rows}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Key" Width="250" DisplayMemberBinding="{Binding Key}" />
                    <GridViewColumn Header="Value" Width="420" DisplayMemberBinding="{Binding Value}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
