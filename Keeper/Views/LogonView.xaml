<Window x:Class="Keeper.Views.LogonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LogonView" Width="300" SizeToContent="Height">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="10"/>
      <RowDefinition Height="30"/>
      <RowDefinition Height="20"/>
      <RowDefinition Height="30"/>
      <RowDefinition Height="30"/>
      <RowDefinition Height="40"/>
      <RowDefinition Height="30"/>
    </Grid.RowDefinitions>
    <TextBlock Grid.Row="1" Text="Keeper (c) 2012-2013 " HorizontalAlignment="Center" Margin="0,5,0,5" FontWeight="Bold"/>
    <TextBlock Grid.Row="3" Text="Введите пароль" Margin="25,5,0,5"/>
    <TextBox Grid.Row="4" Text="{Binding Password, Mode=TwoWay}" HorizontalAlignment="Left" Margin="25,5,0,5" Width="170"/>
    <Button Name="LogIn" Grid.Row="4" Content="OK" HorizontalAlignment="Right" Margin="0,5,25,5" Width="50"/>
    <TextBlock Name="NowStamp" Grid.Row="6" Text="2/56/6819 2:35" HorizontalAlignment="Right" Margin="0,5,25,5"/>
  </Grid>
</Window>
