<Window x:Class="Keeper.Views.TransactionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModels="clr-namespace:Keeper.ViewModels" mc:Ignorable="d"
        Title="TransactionsView" Height="500" 
        d:DataContext="{d:DesignInstance ViewModels:TransactionsViewModel}">
    <Grid>
    <ListView HorizontalAlignment="Left" Margin="5" VerticalAlignment="Top"
              ItemsSource="{Binding Rows}" >
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Timestamp}" />
          <GridViewColumn Header="Дебет" DisplayMemberBinding="{Binding Debet}"/>
          <GridViewColumn Header="Кредит" DisplayMemberBinding="{Binding Credit}" />
          <GridViewColumn Header="Категория" DisplayMemberBinding="{Binding Article}" />
          <GridViewColumn Header="Валюта" DisplayMemberBinding="{Binding Currency}" />
          <GridViewColumn Header="Сумма" DisplayMemberBinding="{Binding Amount}" />
          <GridViewColumn Header="Примечание" DisplayMemberBinding="{Binding Comment}" />
        </GridView>
      </ListView.View>
    </ListView>

  </Grid>
</Window>
