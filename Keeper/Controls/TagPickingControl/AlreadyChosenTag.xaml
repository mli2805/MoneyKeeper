<UserControl x:Class="Keeper.Controls.TagPickingControl.AlreadyChosenTag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             Height="25"
             mc:Ignorable="d"
             cal:Bind.ModelWithoutContext="{Binding .}"
             d:DesignWidth="300">
    <Grid>
        <DockPanel>
            <Button Content="x" Width="20" Height="20" DockPanel.Dock="Left"  VerticalAlignment="Top" 
                    Background="Transparent" BorderBrush="Transparent" Click="DeleteTagOnClick"/>
            <Label Content="{Binding .}" Margin="0,-2,0,0" DockPanel.Dock="Right" />
        </DockPanel>

    </Grid>
</UserControl>
