<UserControl x:Class="Keeper.Controls.ComboboxTreeviewWithButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Keeper.Controls"
             xmlns:comboboxTreeview="clr-namespace:Keeper.Controls.ComboboxTreeview"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <HierarchicalDataTemplate x:Key="TreeViewDataTemplate" ItemsSource="{Binding Children}">
                <TextBlock Text="{Binding Name}" />
            </HierarchicalDataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <StackPanel Name="Buttons" VerticalAlignment="Top" Margin="5,0,0,0" Orientation="Horizontal">
        </StackPanel>
        <comboboxTreeview:ComboBoxTreeView HorizontalAlignment="Stretch" Margin="0,25,0,0" Background="White" 
                      VerticalAlignment="Top" Height="25" ItemTemplate="{StaticResource TreeViewDataTemplate}" 
                      ItemsSource="{Binding RootsListForCombo}" SelectedItem="{Binding MySelectedItem, Mode=TwoWay}" />

    
    </Grid>
</UserControl>
