<UserControl x:Class="Keeper.Controls.BarDiagramControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             >
    <Grid Background="Azure">
    <Grid.RowDefinitions>
      <RowDefinition/>
      <RowDefinition Height="30"/>
    </Grid.RowDefinitions>
    <Popup Name="BarHint" >
      <Border Margin="1" Background="Black">
      <TextBlock Name="BarHintText" VerticalAlignment="Center"></TextBlock>
      </Border>
    </Popup>
      <Image Name="DiagramImage" Stretch="Fill"  
           MouseLeftButtonDown="OnMouseLeftButtonDown" MouseLeftButtonUp="OnMouseLeftButtonUp" 
           MouseRightButtonDown="OnMouseRightButtonDown" MouseRightButtonUp="OnMouseRightButtonUp"
           MouseWheel="OnMouseWheel" KeyDown="OnKeyDown" MouseMove="OnMouseMove" >
    
        <Image.ContextMenu>
        <ContextMenu>
          <MenuItem Header="По месяцам" BorderBrush="{x:Null}" Background="White" Click="GroupByMonthes" />
          <MenuItem Header="По годам" BorderBrush="{x:Null}" Background="White" Click="GroupByYears" />
        </ContextMenu>
      </Image.ContextMenu>
    </Image>
    <TextBlock Name="StatusBar" Grid.Row="1"/>
  </Grid>
</UserControl>
