<UserControl x:Class="Keeper2018.BalanceOrTrafficView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:keeper2018="clr-namespace:Keeper2018"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400" 
             cal:Bind.ModelWithoutContext="{Binding .}"
             d:DataContext="{d:DesignInstance keeper2018:BalanceOrTrafficViewModel}">
    <Grid>
        <ListView Margin="5" ItemsSource="{Binding Lines}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{Binding AccountName}"/>
                </GridView>
            </ListView.View>
        </ListView>    
    </Grid>
</UserControl>
