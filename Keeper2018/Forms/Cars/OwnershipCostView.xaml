<Window x:Class="Keeper2018.OwnershipCostView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        WindowState="Maximized" d:DataContext="{d:DesignData OwnershipCostViewModel}">
    <i:Interaction.Triggers>
        <ei:KeyTrigger Key="T">
            <cal:ActionMessage MethodName="ToggleModel"/>
        </ei:KeyTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <oxy:PlotView Model="{Binding DailyOwnershipCostPlotModel}" Visibility="{Binding DailyVisibility}"/>
        <oxy:PlotView Model="{Binding MonthlyOwnershipCostPlotModel}" Visibility="{Binding MonthlyVisibility}"/>
    </Grid>
</Window>
