<UserControl x:Class="Keeper2018.ListOfLinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:keeper2018="clr-namespace:Keeper2018"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance keeper2018:ListOfLinesViewModel}">
    <ItemsControl ItemsSource="{Binding List.Lines}" Margin="0">
        <ItemsControl.ItemTemplate>
            <DataTemplate DataType="keeper2018:ListLine">
                <TextBlock Text="{Binding Line}" 
                           FontWeight="{Binding FontWeight}" FontSize="{Binding FontSize}" Foreground="{Binding Foreground}"/>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
