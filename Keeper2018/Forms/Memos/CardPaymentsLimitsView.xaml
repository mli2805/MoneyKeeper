<UserControl x:Class="Keeper2018.CardPaymentsLimitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             cal:Bind.ModelWithoutContext="{Binding .}" 
             d:DesignHeight="450" d:DesignWidth="820">
    <DataGrid ItemsSource="{Binding Rows}" AutoGenerateColumns="False">
        <DataGrid.Resources>
            <Style x:Key="WrappedColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <TextBlock TextWrapping="Wrap" Text="{Binding}"></TextBlock>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </DataGrid.Resources>
        <DataGrid.CellStyle>
            <Style TargetType="DataGridCell">
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="Black" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </DataGrid.CellStyle>
        <DataGrid.ItemContainerStyle>
            <Style TargetType="{x:Type DataGridRow}">
                <Setter Property="Background" Value="{Binding RowBackground}" />
                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#B6E6FF" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </DataGrid.ItemContainerStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="Карточка" Width="240" Binding="{Binding Account}"/>
            <DataGridTextColumn Header="Текущий остаток" HeaderStyle="{StaticResource WrappedColumnHeaderStyle}"
                                Width="80" Binding="{Binding CurrentBalance}"/>
            <DataGridTextColumn Header="не меньше" HeaderStyle="{StaticResource WrappedColumnHeaderStyle}"
                                Width="70" Binding="{Binding NotLess}"/>
            <DataGridTextColumn Header="Расходов за период" HeaderStyle="{StaticResource WrappedColumnHeaderStyle}"
                                Width="80" Binding="{Binding CurrentExpense}" FontWeight="Bold"/>
            <DataGridTextColumn Header="не больше" HeaderStyle="{StaticResource WrappedColumnHeaderStyle}"
                                Width="70" Binding="{Binding NotMore}"/>
            <DataGridTextColumn Header="Примечание" Width="600" Binding="{Binding Comment}">
                <DataGridTextColumn.ElementStyle>
                    <Style>
                        <Setter Property="TextBlock.TextWrapping" Value="Wrap" />
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
