<Window x:Class="Keeper2018.MonthlyChartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        WindowState="Maximized" d:DataContext="{d:DesignData MonthlyChartViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <oxy:PlotView Grid.Row="0" Grid.Column="0" Model="{Binding MyPlotModel00}"/>
        <oxy:PlotView Grid.Row="0" Grid.Column="1" Model="{Binding MyPlotModel01}"/>
        <oxy:PlotView Grid.Row="1" Grid.Column="0" Model="{Binding MyPlotModel10}"/>
        <oxy:PlotView Grid.Row="1" Grid.Column="1" Model="{Binding MyPlotModel11}"/>

    </Grid>
</Window>
