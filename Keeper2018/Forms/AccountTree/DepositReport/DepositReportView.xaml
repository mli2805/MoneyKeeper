<Window x:Class="Keeper2018.DepositReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:keeper2018="clr-namespace:Keeper2018"
        mc:Ignorable="d"
        Width="500" d:DataContext="{d:DesignInstance keeper2018:DepositReportViewModel}">
    <DockPanel>
        <StackPanel Name="Header" DockPanel.Dock="Top">
            <TextBlock Text="{Binding Model.DepositState}" Margin="5"/>
            <TextBlock Text="{Binding Model.Balance}" Margin="5"/>
        </StackPanel>
        <StackPanel Name="Buttons" DockPanel.Dock="Bottom">
            <Button Name="Close" Content="Close" Width="100" Margin="15,5" HorizontalAlignment="Right"/>
        </StackPanel>
        <StackPanel Name="Footer" DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding Model.Facts}" Margin="5"/>
            <TextBlock Text="{Binding Model.MonthInterest}" Margin="5"/>
            <TextBlock Text="{Binding Model.AllInterests}" Margin="5"/>
            <TextBlock Text="Итоговый прогноз:" Margin="5"/>
            <TextBlock Text="{Binding Model.Forecast}" Margin="5"/>
        </StackPanel>
        <ListView ItemsSource="{Binding Model.Traffic}"></ListView>
    </DockPanel>
</Window>
