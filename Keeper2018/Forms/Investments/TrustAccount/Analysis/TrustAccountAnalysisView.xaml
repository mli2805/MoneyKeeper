<Window x:Class="Keeper2018.TrustAccountAnalysisView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TrustAccountAnalysisView" Height="450" Width="800" d:DataContext="{d:DesignData TrustAccountAnalysisViewModel}">
    <DockPanel>
        <DataGrid DockPanel.Dock="Top" Margin="5" 
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header=" Тикер" Width="120" Binding="{Binding InvestmentAssetTicker}" />
                <DataGridTextColumn Header=" Количество " Width="120" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header=" Стоимость " Width="120" Binding="{Binding Price}" />
                <DataGridTextColumn Header=" Оплачен купон" Width="120" Binding="{Binding PaidCoupon}" />
                <DataGridTextColumn Header=" Получен купон/дивы" Width="120" Binding="{Binding ReceivedCoupon}" />
                <DataGridTextColumn Header=" Текущая стоимость" Width="120" Binding="{Binding CurrentPrice}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
