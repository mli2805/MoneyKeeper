<Window x:Class="Keeper2018.RusBelChartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        WindowState="Maximized" d:DataContext="{d:DesignData RusBelChartViewModel}">
    <Grid>
        <oxy:PlotView Model="{Binding MyPlotModel}"/>
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,54,110,0">
            <CheckBox Content="Rub / Byn" IsChecked="{Binding IsRubBynVisible}" Height="20"/>
        </StackPanel>
    </Grid>
</Window>

