+ обертка над settings

+ избавиться от статическ филдов в UsefulLists (смотри комбик фильтров на форме курсов)

 и отнести к конкретным вью-моделям - 
  1) но часть списков используется разными моделями!
  2) когда обычный комбобох - отлично работает, но когда DataGridComboBoxColumn 
  (прямо датагрид привязан) не получается комбик на поле во вьюмодели перепривязать
  ассоциации и курсы валют? просто сделал статические комбики в конкретных вьюмоделях - разнес по моделям, но пришлось сделать STATIC 

 Как-то стандартизировать IMyFactory ???


TestAccountTreesGardener

 //			selectedAccount = accountInWork.Parent;
 1) строчка нужна

 2) как заставить дерево сменить селекшен?

 3) accountInWork = new Account() { Parent = mSelectedAccount }; первый же тест
 насколько правомерно исполнение хоть какого-то кода в тесте

 4) слова, которые пишутся одинаково, но произносятся по разному


+ AccountTreeStraightener

     Для FlattenWithLevels используй структурное сравнение ShouldBeEqTo
      // mChild1Account = new Account();
      // mChild2Account = new Account();
      // mParentAccount = new Account {Children = {mChild1Account, mChild2Account}};
     
      // result.ShouldBeAllEqTo(new []{ 
      //     new HierarchyItem<Account>(mParentAccount, 0), 
      //     new HierarchyItem<Account>(mChild1Account, 1), )
      //

+ DbTxtFileWriter
   пришлось расширять IFile

+ DbToTxtSaver


+ yield

? выключение компиляции на ходу? неправильно дает ошибки